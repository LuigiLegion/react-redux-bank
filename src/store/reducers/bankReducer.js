// Initial State
const initialState = {};

// Actions Types

// Action Creators

// Reducer
const bankReducer = (state = initialState, action) => {
  switch (action.type) {
    // Your cases here

    default:
      return state;
  }
};

// Exports
export default bankReducer;
