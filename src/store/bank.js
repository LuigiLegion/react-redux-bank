// Initial State
const initialState = {};

// Actions

// Action Creators

// Reducer
const bankReducer = (state = initialState, action) => {
  switch (action.type) {
    // Your cases here

    default:
      return state;
  }
};

export default bankReducer;
